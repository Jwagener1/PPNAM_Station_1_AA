<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingBottom="16dp">

    <TextView
        android:id="@+id/tvDoneMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/text_primary"
        android:textSize="16sp" />

    <!-- Continue scanning is the everyday action: primary blue. -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnNextPallet"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="20dp"
        android:text="@string/btn_next_pallet"
        android:textColor="@android:color/white"
        app:backgroundTint="@color/primary_action"
        app:cornerRadius="14dp" />

    <!-- Closing the document is the terminal action: success green, clearly not the default. -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnDoneClose"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="12dp"
        android:text="@string/btn_done"
        android:textColor="@android:color/white"
        app:backgroundTint="@color/success"
        app:cornerRadius="14dp" />
</LinearLayout>
